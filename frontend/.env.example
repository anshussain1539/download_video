# Public URL of your backend, with the /api suffix.
# - Leave unset for local dev (Vite proxies /api -> localhost:8000 via vite.config.js).
# - On Vercel, set this in Project Settings -> Environment Variables.
VITE_API_BASE=https://download-video-ilvh.onrender.com/api
